using UnityEngine;

public class PlayerData : MonoBehaviour
{
    public static int health = 100, score = 0;
    public static float speedMult = 1f;
    //public static ItemList[] <-- Future(We need to keep track of current items player has)
}
